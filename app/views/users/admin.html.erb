<h1>admin</h1>

<% @enrollments.each do |enrollment|%>
<div>
    <h2><%= link_to(enrollment.title, enrollment_path(enrollment.id))%></h2>
    <p><%= enrollment.description%></p>
    </div>
</div>
<%end%>